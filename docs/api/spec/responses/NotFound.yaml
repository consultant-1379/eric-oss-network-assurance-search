description: Not found - the resource is not found.
content:
  application/json:
    schema:
      $ref: '../schemas/Error.yaml'
    example:
      {
        "code": 404,
        "message": "Not Found"
      }
